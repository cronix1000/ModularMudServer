#pragma once

// CombatManager.h - Stub header for CombatManager class
// The .cpp file is currently empty/unused. This header is provided
// so the file can be included if/when CombatManager is implemented.

class CombatManager {
public:
    CombatManager();
    ~CombatManager();
};
